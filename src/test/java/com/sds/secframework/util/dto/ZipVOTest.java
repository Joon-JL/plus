package com.sds.secframework.util.dto;

import java.lang.String;
import org.junit.Test;
import com.sds.secframework.common.dto.CommonVO;
import static org.mockito.Mockito.mock;

public class ZipVOTest {

    @Test(timeout = 20000)
    public void testGetZipcodeShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getZipcode();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetZipcodeWithStringZipcodeShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String zipcode = "zipcode";
        boolean isNotException = true;
        try {
            clazz.setZipcode(zipcode);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSeqShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getSeq();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSeqWithStringSeqShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String seq = "seq";
        boolean isNotException = true;
        try {
            clazz.setSeq(seq);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSidoShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getSido();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSidoWithStringSidoShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String sido = "sido";
        boolean isNotException = true;
        try {
            clazz.setSido(sido);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSigunguShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getSigungu();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSigunguWithStringSigunguShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String sigungu = "sigungu";
        boolean isNotException = true;
        try {
            clazz.setSigungu(sigungu);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDongShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getDong();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDongWithStringDongShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String dong = "dong";
        boolean isNotException = true;
        try {
            clazz.setDong(dong);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRiShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getRi();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRiWithStringRiShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String ri = "ri";
        boolean isNotException = true;
        try {
            clazz.setRi(ri);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDoseoShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getDoseo();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDoseoWithStringDoseoShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String doseo = "doseo";
        boolean isNotException = true;
        try {
            clazz.setDoseo(doseo);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetBunjiShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getBunji();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetBunjiWithStringBunjiShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String bunji = "bunji";
        boolean isNotException = true;
        try {
            clazz.setBunji(bunji);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetBldgShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getBldg();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetBldgWithStringBldgShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String bldg = "bldg";
        boolean isNotException = true;
        try {
            clazz.setBldg(bldg);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetChange_ymdShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getChange_ymd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetChange_ymdWithStringChangeYmdShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String change_ymd = "change_ymd";
        boolean isNotException = true;
        try {
            clazz.setChange_ymd(change_ymd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetAddressShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getAddress();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAddressWithStringAddressShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String address = "address";
        boolean isNotException = true;
        try {
            clazz.setAddress(address);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDongnmShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        boolean isNotException = true;
        try {
            clazz.getDongnm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDongnmWithStringDongnmShouldNotThrowException() {
        ZipVO clazz = new ZipVO();
        String dongnm = "dongnm";
        boolean isNotException = true;
        try {
            clazz.setDongnm(dongnm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

}
