package com.sec.las.lawservice.dto;

import java.lang.String;
import org.junit.Test;
import static org.mockito.Mockito.mock;

public class LawfirmRemitCertVOTest {

    @Test(timeout = 20000)
    public void testGetUpfile_ynShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getUpfile_yn();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetUpfile_ynWithStringUpfileYnShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String upfile_yn = "upfile_yn";
        boolean isNotException = true;
        try {
            clazz.setUpfile_yn(upfile_yn);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRemitcert_noShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getRemitcert_no();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRemitcert_noWithStringRemitcertNoShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String remitcert_no = "remitcert_no";
        boolean isNotException = true;
        try {
            clazz.setRemitcert_no(remitcert_no);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetLawfirm_idShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getLawfirm_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetLawfirm_idWithStringLawfirmIdShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String lawfirm_id = "lawfirm_id";
        boolean isNotException = true;
        try {
            clazz.setLawfirm_id(lawfirm_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRemitcert_nmShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getRemitcert_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRemitcert_nmWithStringRemitcertNmShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String remitcert_nm = "remitcert_nm";
        boolean isNotException = true;
        try {
            clazz.setRemitcert_nm(remitcert_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRemitdayShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getRemitday();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRemitdayWithStringRemitdayShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String remitday = "remitday";
        boolean isNotException = true;
        try {
            clazz.setRemitday(remitday);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetInvoice_noShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getInvoice_no();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetInvoice_noWithStringInvoiceNoShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String invoice_no = "invoice_no";
        boolean isNotException = true;
        try {
            clazz.setInvoice_no(invoice_no);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetContShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getCont();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetContWithStringContShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String cont = "cont";
        boolean isNotException = true;
        try {
            clazz.setCont(cont);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetLawfirm_nmShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        boolean isNotException = true;
        try {
            clazz.getLawfirm_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetLawfirm_nmWithStringLawfirmNmShouldNotThrowException() {
        LawfirmRemitCertVO clazz = new LawfirmRemitCertVO();
        String lawfirm_nm = "lawfirm_nm";
        boolean isNotException = true;
        try {
            clazz.setLawfirm_nm(lawfirm_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

}
