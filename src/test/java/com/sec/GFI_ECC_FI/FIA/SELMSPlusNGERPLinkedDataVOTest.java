package com.sec.GFI_ECC_FI.FIA;

import java.lang.String;
import org.junit.Test;
import static org.mockito.Mockito.mock;

public class SELMSPlusNGERPLinkedDataVOTest {

    @Test(timeout = 20000)
    public void testSetSELMSPlusIDWithStringSelmsPlusIDShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String selmsPlusID = "selmsPlusID";
        boolean isNotException = true;
        try {
            clazz.setSELMSPlusID(selmsPlusID);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSELMSPlusIDShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getSELMSPlusID();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCompCDWithStringCompCdShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String compCd = "compCd";
        boolean isNotException = true;
        try {
            clazz.setCompCD(compCd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCompCDShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getCompCD();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetBACDWithStringBaCdShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String baCd = "baCd";
        boolean isNotException = true;
        try {
            clazz.setBACD(baCd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetBACDShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getBACD();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetGERPIDWithStringGerpIDShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String gerpID = "gerpID";
        boolean isNotException = true;
        try {
            clazz.setGERPID(gerpID);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetGERPIDShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getGERPID();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetLinkedWithStringLinkedShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String linked = "linked";
        boolean isNotException = true;
        try {
            clazz.setLinked(linked);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetLinkedShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getLinked();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetLinkedDateTimeWithStringLinkedDateTimeShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String linkedDateTime = "linkedDateTime";
        boolean isNotException = true;
        try {
            clazz.setLinkedDateTime(linkedDateTime);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetLinkedDateTimeShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getLinkedDateTime();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetModuleWithStringModuleShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String module = "module";
        boolean isNotException = true;
        try {
            clazz.setModule(module);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetModuleShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getModule();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAmountWithFloatAmountShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        float amount = 0.0f;
        boolean isNotException = true;
        try {
            clazz.setAmount(amount);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetAmountShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getAmount();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCurrencyWithStringCurrencyShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        String currency = "currency";
        boolean isNotException = true;
        try {
            clazz.setCurrency(currency);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCurrencyShouldNotThrowException() {
        SELMSPlusNGERPLinkedDataVO clazz = new SELMSPlusNGERPLinkedDataVO();
        boolean isNotException = true;
        try {
            clazz.getCurrency();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

}
