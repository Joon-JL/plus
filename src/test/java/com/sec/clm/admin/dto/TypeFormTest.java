package com.sec.clm.admin.dto;

import com.sds.secframework.common.dto.CommonForm;
import java.lang.String;
import org.junit.Test;
import static org.mockito.Mockito.mock;

public class TypeFormTest {

    @Test(timeout = 20000)
    public void testGetAuth_insertShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getAuth_insert();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAuth_insertWithBooleanAuthInsertShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean auth_insert = false;
        boolean isNotException = true;
        try {
            clazz.setAuth_insert(auth_insert);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetAuth_modifyShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getAuth_modify();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAuth_modifyWithBooleanAuthModifyShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean auth_modify = false;
        boolean isNotException = true;
        try {
            clazz.setAuth_modify(auth_modify);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testIsAuth_deleteShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.isAuth_delete();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAuth_deleteWithBooleanAuthDeleteShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean auth_delete = false;
        boolean isNotException = true;
        try {
            clazz.setAuth_delete(auth_delete);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSrch_dimensionShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getSrch_dimension();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSrch_dimensionWithStringSrchDimensionShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String srch_dimension = "srch_dimension";
        boolean isNotException = true;
        try {
            clazz.setSrch_dimension(srch_dimension);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSrch_wordShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getSrch_word();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSrch_wordWithStringSrchWordShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String srch_word = "srch_word";
        boolean isNotException = true;
        try {
            clazz.setSrch_word(srch_word);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetStart_indexShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getStart_index();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetStart_indexWithStringStartIndexShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String start_index = "start_index";
        boolean isNotException = true;
        try {
            clazz.setStart_index(start_index);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetEnd_indexShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getEnd_index();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetEnd_indexWithStringEndIndexShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String end_index = "end_index";
        boolean isNotException = true;
        try {
            clazz.setEnd_index(end_index);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCurPageShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getCurPage();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCurPageWithStringCurPageShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String curPage = "curPage";
        boolean isNotException = true;
        try {
            clazz.setCurPage(curPage);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRow_cntShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getRow_cnt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRow_cntWithIntRowCntShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        int row_cnt = 0;
        boolean isNotException = true;
        try {
            clazz.setRow_cnt(row_cnt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDimension_cdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getDimension_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDimension_cdWithStringDimensionCdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String dimension_cd = "dimension_cd";
        boolean isNotException = true;
        try {
            clazz.setDimension_cd(dimension_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDetail_cdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getDetail_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDetail_cdWithStringDetailCdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String detail_cd = "detail_cd";
        boolean isNotException = true;
        try {
            clazz.setDetail_cd(detail_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetType_cdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getType_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetType_cdWithStringTypeCdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String type_cd = "type_cd";
        boolean isNotException = true;
        try {
            clazz.setType_cd(type_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDimension_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getDimension_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDimension_nmWithStringDimensionNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String dimension_nm = "dimension_nm";
        boolean isNotException = true;
        try {
            clazz.setDimension_nm(dimension_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDetail_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getDetail_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDetail_nmWithStringDetailNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String detail_nm = "detail_nm";
        boolean isNotException = true;
        try {
            clazz.setDetail_nm(detail_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCd_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getCd_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCd_nmWithStringCdNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String cd_nm = "cd_nm";
        boolean isNotException = true;
        try {
            clazz.setCd_nm(cd_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCd_nm_abbrShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getCd_nm_abbr();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCd_nm_abbrWithStringCdNmAbbrShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String cd_nm_abbr = "cd_nm_abbr";
        boolean isNotException = true;
        try {
            clazz.setCd_nm_abbr(cd_nm_abbr);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCd_nm_engShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getCd_nm_eng();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCd_nm_engWithStringCdNmEngShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String cd_nm_eng = "cd_nm_eng";
        boolean isNotException = true;
        try {
            clazz.setCd_nm_eng(cd_nm_eng);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCd_nm_abbr_engShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getCd_nm_abbr_eng();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCd_nm_abbr_engWithStringCdNmAbbrEngShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String cd_nm_abbr_eng = "cd_nm_abbr_eng";
        boolean isNotException = true;
        try {
            clazz.setCd_nm_abbr_eng(cd_nm_abbr_eng);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetPrnt_srtShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getPrnt_srt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetPrnt_srtWithStringPrntSrtShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String prnt_srt = "prnt_srt";
        boolean isNotException = true;
        try {
            clazz.setPrnt_srt(prnt_srt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCd_explShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getCd_expl();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCd_explWithStringCdExplShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String cd_expl = "cd_expl";
        boolean isNotException = true;
        try {
            clazz.setCd_expl(cd_expl);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetUse_ynShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getUse_yn();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetUse_ynWithStringUseYnShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String use_yn = "use_yn";
        boolean isNotException = true;
        try {
            clazz.setUse_yn(use_yn);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetReg_dtShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getReg_dt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetReg_dtWithStringRegDtShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String reg_dt = "reg_dt";
        boolean isNotException = true;
        try {
            clazz.setReg_dt(reg_dt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetReg_idShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getReg_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetReg_idWithStringRegIdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String reg_id = "reg_id";
        boolean isNotException = true;
        try {
            clazz.setReg_id(reg_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetReg_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getReg_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetReg_nmWithStringRegNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String reg_nm = "reg_nm";
        boolean isNotException = true;
        try {
            clazz.setReg_nm(reg_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetReg_dept_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getReg_dept_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetReg_dept_nmWithStringRegDeptNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String reg_dept_nm = "reg_dept_nm";
        boolean isNotException = true;
        try {
            clazz.setReg_dept_nm(reg_dept_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMod_dtShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getMod_dt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMod_dtWithStringModDtShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String mod_dt = "mod_dt";
        boolean isNotException = true;
        try {
            clazz.setMod_dt(mod_dt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMod_idShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getMod_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMod_idWithStringModIdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String mod_id = "mod_id";
        boolean isNotException = true;
        try {
            clazz.setMod_id(mod_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMod_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getMod_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMod_nmWithStringModNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String mod_nm = "mod_nm";
        boolean isNotException = true;
        try {
            clazz.setMod_nm(mod_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMod_dept_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getMod_dept_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMod_dept_nmWithStringModDeptNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String mod_dept_nm = "mod_dept_nm";
        boolean isNotException = true;
        try {
            clazz.setMod_dept_nm(mod_dept_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cdWithStringItemCdShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String item_cd = "item_cd";
        boolean isNotException = true;
        try {
            clazz.setItem_cd(item_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cdsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cds();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cdsWithStringArrayItemCdsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String[] item_cds = mock(String[].class);
        boolean isNotException = true;
        try {
            clazz.setItem_cds(item_cds);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nmWithStringItemCdNmShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String item_cd_nm = "item_cd_nm";
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm(item_cd_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nmsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nms();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nmsWithStringArrayItemCdNmsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String[] item_cd_nms = mock(String[].class);
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nms(item_cd_nms);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nm_abbrShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm_abbr();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nm_abbrWithStringItemCdNmAbbrShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String item_cd_nm_abbr = "item_cd_nm_abbr";
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm_abbr(item_cd_nm_abbr);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nm_abbrsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm_abbrs();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nm_abbrsWithStringArrayItemCdNmAbbrsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String[] item_cd_nm_abbrs = mock(String[].class);
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm_abbrs(item_cd_nm_abbrs);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nm_engShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm_eng();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nm_engWithStringItemCdNmEngShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String item_cd_nm_eng = "item_cd_nm_eng";
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm_eng(item_cd_nm_eng);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nm_engsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm_engs();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nm_engsWithStringArrayItemCdNmEngsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String[] item_cd_nm_engs = mock(String[].class);
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm_engs(item_cd_nm_engs);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nm_abbr_engShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm_abbr_eng();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nm_abbr_engWithStringItemCdNmAbbrEngShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String item_cd_nm_abbr_eng = "item_cd_nm_abbr_eng";
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm_abbr_eng(item_cd_nm_abbr_eng);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetItem_cd_nm_abbr_engsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        boolean isNotException = true;
        try {
            clazz.getItem_cd_nm_abbr_engs();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetItem_cd_nm_abbr_engsWithStringArrayItemCdNmAbbrEngsShouldNotThrowException() {
        TypeForm clazz = new TypeForm();
        String[] item_cd_nm_abbr_engs = mock(String[].class);
        boolean isNotException = true;
        try {
            clazz.setItem_cd_nm_abbr_engs(item_cd_nm_abbr_engs);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

}
