package com.sec.clm.admin.dto;

import com.sds.secframework.common.dto.CommonForm;
import java.lang.String;
import org.junit.Test;
import static org.mockito.Mockito.mock;

public class ApprovalPathFormTest {

    @Test(timeout = 20000)
    public void testGetSrch_path_nameShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getSrch_path_name();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSrch_path_nameWithStringSrchPathNameShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String srch_path_name = "srch_path_name";
        boolean isNotException = true;
        try {
            clazz.setSrch_path_name(srch_path_name);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSrch_use_ynShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getSrch_use_yn();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSrch_use_ynWithStringSrchUseYnShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String srch_use_yn = "srch_use_yn";
        boolean isNotException = true;
        try {
            clazz.setSrch_use_yn(srch_use_yn);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSrch_loc_gbnShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getSrch_loc_gbn();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSrch_loc_gbnWithStringSrchLocGbnShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String srch_loc_gbn = "srch_loc_gbn";
        boolean isNotException = true;
        try {
            clazz.setSrch_loc_gbn(srch_loc_gbn);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetStart_indexShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getStart_index();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetStart_indexWithStringStartIndexShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String start_index = "start_index";
        boolean isNotException = true;
        try {
            clazz.setStart_index(start_index);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetEnd_indexShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getEnd_index();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetEnd_indexWithStringEndIndexShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String end_index = "end_index";
        boolean isNotException = true;
        try {
            clazz.setEnd_index(end_index);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCurPageShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCurPage();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCurPageWithStringCurPageShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String curPage = "curPage";
        boolean isNotException = true;
        try {
            clazz.setCurPage(curPage);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRow_cntShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getRow_cnt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRow_cntWithIntRowCntShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        int row_cnt = 0;
        boolean isNotException = true;
        try {
            clazz.setRow_cnt(row_cnt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRole_nameShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getRole_name();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRole_nameWithStringRoleNameShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String role_name = "role_name";
        boolean isNotException = true;
        try {
            clazz.setRole_name(role_name);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetReg_dtShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getReg_dt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetReg_dtWithStringRegDtShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String reg_dt = "reg_dt";
        boolean isNotException = true;
        try {
            clazz.setReg_dt(reg_dt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetReg_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getReg_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetReg_idWithStringRegIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String reg_id = "reg_id";
        boolean isNotException = true;
        try {
            clazz.setReg_id(reg_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCommentsShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getComments();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCommentsWithStringCommentsShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String comments = "comments";
        boolean isNotException = true;
        try {
            clazz.setComments(comments);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetComp_cdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getComp_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetComp_cdWithStringCompCdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String comp_cd = "comp_cd";
        boolean isNotException = true;
        try {
            clazz.setComp_cd(comp_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRole_cdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getRole_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRole_cdWithStringRoleCdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String role_cd = "role_cd";
        boolean isNotException = true;
        try {
            clazz.setRole_cd(role_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetUse_ynShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getUse_yn();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetUse_ynWithStringUseYnShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String use_yn = "use_yn";
        boolean isNotException = true;
        try {
            clazz.setUse_yn(use_yn);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetUser_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getUser_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetUser_idWithStringUserIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String user_id = "user_id";
        boolean isNotException = true;
        try {
            clazz.setUser_id(user_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetUser_id_tmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getUser_id_tmp();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetUser_id_tmpWithStringUserIdTmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String user_id_tmp = "user_id_tmp";
        boolean isNotException = true;
        try {
            clazz.setUser_id_tmp(user_id_tmp);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMod_dtShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getMod_dt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMod_dtWithStringModDtShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String mod_dt = "mod_dt";
        boolean isNotException = true;
        try {
            clazz.setMod_dt(mod_dt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMod_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getMod_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMod_idWithStringModIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String mod_id = "mod_id";
        boolean isNotException = true;
        try {
            clazz.setMod_id(mod_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDel_dtShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getDel_dt();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDel_dtWithStringDelDtShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String del_dt = "del_dt";
        boolean isNotException = true;
        try {
            clazz.setDel_dt(del_dt);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetDel_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getDel_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetDel_idWithStringDelIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String del_id = "del_id";
        boolean isNotException = true;
        try {
            clazz.setDel_id(del_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetAss_id_listShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getAss_id_list();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAss_id_listWithStringAssIdListShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String ass_id_list = "ass_id_list";
        boolean isNotException = true;
        try {
            clazz.setAss_id_list(ass_id_list);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetUp_type_cdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getUp_type_cd();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetUp_type_cdWithStringUpTypeCdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String up_type_cd = "up_type_cd";
        boolean isNotException = true;
        try {
            clazz.setUp_type_cd(up_type_cd);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetType_1ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getType_1();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetType_1WithStringType1ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String type_1 = "type_1";
        boolean isNotException = true;
        try {
            clazz.setType_1(type_1);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetType_2ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getType_2();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetType_2WithStringType2ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String type_2 = "type_2";
        boolean isNotException = true;
        try {
            clazz.setType_2(type_2);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetType_3ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getType_3();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetType_3WithStringType3ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String type_3 = "type_3";
        boolean isNotException = true;
        try {
            clazz.setType_3(type_3);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetType_4ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getType_4();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetType_4WithStringType4ShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String type_4 = "type_4";
        boolean isNotException = true;
        try {
            clazz.setType_4(type_4);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetPath_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getPath_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetPath_idWithStringPathIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String path_id = "path_id";
        boolean isNotException = true;
        try {
            clazz.setPath_id(path_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetPath_nmShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getPath_nm();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetPath_nmWithStringPathNmShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String path_nm = "path_nm";
        boolean isNotException = true;
        try {
            clazz.setPath_nm(path_nm);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetPriorityShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getPriority();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetPriorityWithStringPriorityShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String priority = "priority";
        boolean isNotException = true;
        try {
            clazz.setPriority(priority);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetMandatoryShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getMandatory();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetMandatoryWithStringMandatoryShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String mandatory = "mandatory";
        boolean isNotException = true;
        try {
            clazz.setMandatory(mandatory);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCondition_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCondition_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCondition_idWithIntConditionIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        int condition_id = 0;
        boolean isNotException = true;
        try {
            clazz.setCondition_id(condition_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetOperationShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getOperation();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetOperationWithStringOperationShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String operation = "operation";
        boolean isNotException = true;
        try {
            clazz.setOperation(operation);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetConditionShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCondition();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetConditionWithStringConditionShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String condition = "condition";
        boolean isNotException = true;
        try {
            clazz.setCondition(condition);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetC_optionShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getC_option();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetC_optionWithStringCOptionShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String c_option = "c_option";
        boolean isNotException = true;
        try {
            clazz.setC_option(c_option);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetC_valShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getC_val();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetC_valWithStringCValShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String c_val = "c_val";
        boolean isNotException = true;
        try {
            clazz.setC_val(c_val);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCodition_chkShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCodition_chk();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCodition_chkWithStringCoditionChkShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String codition_chk = "codition_chk";
        boolean isNotException = true;
        try {
            clazz.setCodition_chk(codition_chk);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetInput_condi_listShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getInput_condi_list();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetInput_condi_listWithStringInputCondiListShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String input_condi_list = "input_condi_list";
        boolean isNotException = true;
        try {
            clazz.setInput_condi_list(input_condi_list);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetInput_condi_valShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getInput_condi_val();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetInput_condi_valWithStringInputCondiValShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String input_condi_val = "input_condi_val";
        boolean isNotException = true;
        try {
            clazz.setInput_condi_val(input_condi_val);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetInput_condi_optionShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getInput_condi_option();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetInput_condi_optionWithStringInputCondiOptionShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String input_condi_option = "input_condi_option";
        boolean isNotException = true;
        try {
            clazz.setInput_condi_option(input_condi_option);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetA_typeShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getA_type();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetA_typeWithStringATypeShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String a_type = "a_type";
        boolean isNotException = true;
        try {
            clazz.setA_type(a_type);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetR_typeShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getR_type();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetR_typeWithStringRTypeShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String r_type = "r_type";
        boolean isNotException = true;
        try {
            clazz.setR_type(r_type);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRole_user_idShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getRole_user_id();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRole_user_idWithStringRoleUserIdShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String role_user_id = "role_user_id";
        boolean isNotException = true;
        try {
            clazz.setRole_user_id(role_user_id);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCondi_list_tmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCondi_list_tmp();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCondi_list_tmpWithStringCondiListTmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String condi_list_tmp = "condi_list_tmp";
        boolean isNotException = true;
        try {
            clazz.setCondi_list_tmp(condi_list_tmp);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCondi_val_tmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCondi_val_tmp();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCondi_val_tmpWithStringCondiValTmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String condi_val_tmp = "condi_val_tmp";
        boolean isNotException = true;
        try {
            clazz.setCondi_val_tmp(condi_val_tmp);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetCondi_option_tmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getCondi_option_tmp();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetCondi_option_tmpWithStringCondiOptionTmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String condi_option_tmp = "condi_option_tmp";
        boolean isNotException = true;
        try {
            clazz.setCondi_option_tmp(condi_option_tmp);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetAtype_valShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getAtype_val();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAtype_valWithStringAtypeValShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String atype_val = "atype_val";
        boolean isNotException = true;
        try {
            clazz.setAtype_val(atype_val);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRtype_valShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getRtype_val();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRtype_valWithStringRtypeValShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String rtype_val = "rtype_val";
        boolean isNotException = true;
        try {
            clazz.setRtype_val(rtype_val);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetAtype_val_tmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getAtype_val_tmp();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetAtype_val_tmpWithStringAtypeValTmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String atype_val_tmp = "atype_val_tmp";
        boolean isNotException = true;
        try {
            clazz.setAtype_val_tmp(atype_val_tmp);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetRtype_val_tmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getRtype_val_tmp();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetRtype_val_tmpWithStringRtypeValTmpShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String rtype_val_tmp = "rtype_val_tmp";
        boolean isNotException = true;
        try {
            clazz.setRtype_val_tmp(rtype_val_tmp);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetSort_noShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getSort_no();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetSort_noWithStringSortNoShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String sort_no = "sort_no";
        boolean isNotException = true;
        try {
            clazz.setSort_no(sort_no);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testGetLoc_gbnShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        boolean isNotException = true;
        try {
            clazz.getLoc_gbn();
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

    @Test(timeout = 20000)
    public void testSetLoc_gbnWithStringLocGbnShouldNotThrowException() {
        ApprovalPathForm clazz = new ApprovalPathForm();
        String loc_gbn = "loc_gbn";
        boolean isNotException = true;
        try {
            clazz.setLoc_gbn(loc_gbn);
        } catch (Exception __e) {
            isNotException = false;
        }
        assert isNotException;
    }

}
